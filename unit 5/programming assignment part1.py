def my_sqrt(a): # defining the encapsulation function my_sqrt()
    x = 1
    while True:
        y = (x + a/x) / 2.0
        if y == x:
            break
        x = y 

    return y # returning an y = estimate of the square root

    while True:

        while 1 > 0:

            break

        print("Got it!")

        break 
    
    
